# dotfiles2 apt package list (Debian/Ubuntu/WSL)
#
# 実行: ./setup.sh install
# コメント行(#始まり)と空行は無視される。
#
# 置き換え方針:
# - Brewfile の CLI 相当のみ。GUI/cask/fonts は除外。
# - GNU coreutils 系は Linux は元々 GNU なので不要。
# - apt に無い / 新しい版が欲しいもの (peco, starship, fisher, gh 等) は
#   install/common-post.sh で別ルート導入する。

# 基本ビルドツール
build-essential
pkg-config
ca-certificates
curl
wget
unzip
git

# シェル / エディタ
# zsh がメイン。fish は移行期間中の併存用に残している。
# sheldon (zsh プラグインマネージャ) は apt に無いので common-post.sh で導入する。
# eza は古いディストリの apt に無いことがあるため入れていない
# (zsh/conf.d/50-alias.zsh 側が未導入でも壊れないようガードしてある)。
zsh
fish
neovim
tmux

# CLI ユーティリティ
jq
tree
fzf
ripgrep
fd-find
bat
zoxide

# ネットワーク / 開発補助
openssl
gnupg
